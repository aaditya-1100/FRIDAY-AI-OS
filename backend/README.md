# FRIDAY Backend

This directory contains the backend architecture for the FRIDAY project.

## Testing

Always run tests using Python's module launcher to ensure the correct virtual environment interpreter is used (avoid running `pytest` directly as it may resolve to a global system interpreter):

```bash
..\.venv\Scripts\python -m pytest -v --tb=short
```
